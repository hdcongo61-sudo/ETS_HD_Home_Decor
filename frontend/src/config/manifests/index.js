/**
 * Manifest files (MF) for ETS HD SaaS.
 * Import these when you need feature flags, dynamic navigation, or SaaS branding.
 * No business logic is changed by these manifests.
 */

import featureManifest from './feature-manifest.json';
import modulesManifest from './modules-manifest.json';
import saasManifest from './saas-manifest.json';

export { featureManifest, modulesManifest, saasManifest };

export default {
  featureManifest,
  modulesManifest,
  saasManifest,
};
